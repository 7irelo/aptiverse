name: CI / Build Marketplace Service

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'

    - name: Build Docker image
      run: |
        docker build -t marketplace-service .
        docker save marketplace-service | gzip > marketplace-service-image.tar.gz

    - name: Upload build artifact
      uses: actions/upload-artifact@v4
      with:
        name: marketplace-service-image
        path: marketplace-service-image.tar.gz
        retention-days: 1

    - name: List image info
      run: |
        echo "Docker image built successfully:"
        docker images marketplace-service
        echo "Artifact created: marketplace-service-image.tar.gz"