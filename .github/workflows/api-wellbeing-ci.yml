name: CI / Build Wellbeing Service

on:
  push:
    branches: [ main ]
    paths:
      - "api/wellbeing-service/**"
      - ".github/workflows/api-wellbeing-ci.yml"
  pull_request:
    branches: [ main ]
    paths:
      - "api/wellbeing-service/**"
      - ".github/workflows/api-wellbeing-ci.yml"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: api/wellbeing-service
          push: ${{ github.ref == 'refs/heads/main' && github.event_name == 'push' }}
          tags: |
            7irelo/aptiverse-wellbeing-service:latest
            7irelo/aptiverse-wellbeing-service:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
